spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 192.168.43.156:8848
      config:
        namespace: dev
        group: ulearn
        file-extension: yaml
        refresh-enabled: true

  #profiles默认为dev
  profiles:
    active: dev